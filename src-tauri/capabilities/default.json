{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The editor and its companion authoring-play window can use the local project bridge.",
  "windows": ["main", "authoring-play"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "dialog:allow-open"
  ]
}
